import AuthForm from "../components/auth/AuthForm";
// Registration route enables level selection and password confirmation.
export default function RegisterPage() {
  return <AuthForm register />;
}
